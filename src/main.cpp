#include "man_engine.hpp"
#include "auxiliar.hpp"
#include <cstdio>

int main()
{
    //testing();            // -> This function is an example of how to use the "pass_manager_engine" class

    passwords_manager();    // -> This is a passwords manager program built using the "pass_manager_engine" class

//    char a = 'r', b = 'z', c = 'p';
//    encryptor(a);
//    encryptor(b);
//    std::cout << (int)a << '/' << (int)b << '/' << (int)c << std::endl;
//    std::cout << (int)encryptor(a) << '/' << (int)encryptor(b) << '/' << (int)encryptor(c) << std::endl;
//    std::cout << (int)decryptor(encryptor(a)) << '/' << (int)decryptor(encryptor(b)) << '/' << (int)decryptor(encryptor(c)) << std::endl;
//    for(int i = -128; i < 128; i++) std::cout << i << " - " << (char)i << std::endl;
//        std::cout << sizeof("d") << '/' << (char)164 << '/' << (char)36 << std::endl;
}

// Corregir nombres de registro repetidos
